---
name: simplify
description: Strip designs to their essence by removing unnecessary complexity. Great design is simple, powerful, and clean.
---

Invoke the `simplify` skill from the Soloship plugin. Use the Skill tool with skill name `simplify` and let it drive the workflow.

If the Skill tool cannot find `simplify` by that exact name, read `${CLAUDE_PLUGIN_ROOT}/skills/simplify/SKILL.md` directly (or fall back to `~/.claude/plugins/marketplaces/soloship/skills/simplify/SKILL.md`) and execute its instructions as a complete workflow.

The skill file is the source of truth. Do not paraphrase or improvise — open it and follow it.
