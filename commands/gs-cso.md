---
name: gs-cso
description: Chief Security Officer mode. Infrastructure-first security audit: secrets archaeology, dependency supply chain, CI/CD pipeline security, LLM/AI security, skill supply chain scanning, plus OWASP Top 10, STRIDE threat modeling, and active verification. Two modes: daily (zero-noise, 8/10 confidence gate) and comprehensive (monthly deep scan, 2/10 bar). Trend tracking across audit runs. Use when: "security audit", "threat model", "pentest review", "OWASP", "CSO review". Voice triggers (
---

Invoke the `gs-cso` skill from the Soloship plugin. Use the Skill tool with skill name `gs-cso` and let it drive the workflow.

If the Skill tool cannot find `gs-cso` by that exact name, read `${CLAUDE_PLUGIN_ROOT}/skills/gs-cso/SKILL.md` directly (or fall back to `~/.claude/plugins/marketplaces/soloship/skills/gs-cso/SKILL.md`) and execute its instructions as a complete workflow.

The skill file is the source of truth. Do not paraphrase or improvise — open it and follow it.
