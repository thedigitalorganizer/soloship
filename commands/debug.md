---
name: debug
description: Systematic debugging with root cause discipline. Routes to systematic-debugging. Iron law: no fixes without root cause investigation.
---

Invoke the `debug` skill from the Soloship plugin. Use the Skill tool with skill name `debug` and let it drive the workflow.

If the Skill tool cannot find `debug` by that exact name, read `${CLAUDE_PLUGIN_ROOT}/skills/debug/SKILL.md` directly (or fall back to `~/.claude/plugins/marketplaces/soloship/skills/debug/SKILL.md`) and execute its instructions as a complete workflow.

The skill file is the source of truth. Do not paraphrase or improvise — open it and follow it.
