node_modules/
dist/
# The gs-browse skill ships its compiled launcher binary inside the plugin —
# users get the prebuilt artifact via the Claude Code plugin clone, so dist/
# must be checked in for that skill specifically. Other dist/ dirs stay ignored.
!skills/gs-browse/dist/
!skills/gs-browse/dist/**
.ai/
**/.DS_Store

# Wrangler local state — not for version control
.wrangler/

# Serena MCP local state — not for version control
.serena/

# Internal working files — excluded from public repo
CLAUDE.md
docs/
site/
.claude/settings.local.json
.gstack/
.soloship/
